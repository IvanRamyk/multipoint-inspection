[project]
name = "drone-inspection-dreamer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "gymnasium>=0.29",
    "numpy",
    "matplotlib",
    "pyyaml",
    "pyflyt>=0.29",
]

[project.optional-dependencies]
dreamer = [
    "sheeprl",
    "lightning",
    "tensorboard",
    "torch>=2.4.1,<2.6",
    "torchvision>=0.19,<0.21",
    "imageio[ffmpeg]",
    "cosysairsim",
]
dev = ["jupyter", "ipykernel"]

[tool.setuptools.packages.find]
include = ["envs*", "eval*"]
